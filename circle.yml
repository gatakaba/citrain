machine:
  python:
    version: 3.5.0


    
dependencies:
  pre:
    - pip install -r ./requirements.txt

    
test:
    pre:
        - pep8 hoge
    override:
        - python -V
        - pip list
        - pwd ~
        - ifconfig
        - echo hello world
        - exit 0
