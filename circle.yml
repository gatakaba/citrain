machine:
    post:
        - pyenv install --skip-existing 3.5.0
        - pyenv local  3.5.0

    
dependencies:
  pre:
    - pip install -r ./requirements.txt

    
test:
    pre:
        - pep8 hoge
    override:
        - python -V
        - pwd ~
        - ifconfig
        - echo hello world
        - exit 0
