machine:
  post:
    - pyenv global 2.7.9 3.4.2

    
dependencies:
  pre:
    - pip install -r ./requirements.txt
    
test:
    pre:
        - pep8 hoge
    override:
        - python -V
        - pwd ~
        - ifconfig
        - echo hello world
        - exit 0
