machine:
  python:

    
dependencies:
  pre:
    - pip install -r ./requirements.txt
    
test:
    pre:
        - pep8 hoge
    override:
        - python -v
        - pwd ~
        - ifconfig
        - echo hello world
        - exit 0
